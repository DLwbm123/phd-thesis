# 模板迁移说明

本目录以 `template-v1.1` 为排版基础，迁移自 GitHub 已同步论文内容树 `bb6014163094ad33e968c13f41ef75a73b0505a6`，并保留了随后在 Overleaf 上更新的附录范围（“其他参与论文”、不含学术服务）。

已迁移的编译输入包括：中英文摘要、六章正文、附录 A（在学期间学术成果与科研情况）、参考文献库、全部已引用图表及 TikZ 源文件。模板自带的类文件、字体和复旦标识资源保持为排版基础。

封面信息已保持为：博士、专业学位、学号 `21110980022`、院系“大数据学院”、专业“电子信息”。按照已确认的封面要求，专业学位封面在“院系”下仍显示“专业”，而非“专业学位类别（领域）”。

为使模板承载既有论文内容，`FDSDSthesis.cls` 只做了两项兼容性调整：不再加载与 `algorithm2e` 同名浮动体冲突的旧 `algorithm`/`algorithmic` 包；以及统一专业学位封面的“专业”字段标签。未改写任何学术正文、图表、数据或参考文献条目。

编译命令：

```sh
make
```

该命令使用 XeLaTeX 和 BibTeX，输出为 `main.pdf`。
