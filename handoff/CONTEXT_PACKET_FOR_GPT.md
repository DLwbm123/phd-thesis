# GPT Pro 上下文包：第一章批准后的第二章准备

## 当前任务

题目、连续六章框架和第一章已于 2026-07-31 通过 GPT Pro 学术复审，并已批准为 `drafted_and_verified`。下一步由 Codex 准备第二章 2.1.2“医学图像配准”的证据上下文包；本轮不得生成该节或任何后续学术正文。论文工作题目为“面向任务持续演化与训练信息受限的医学影像持续学习研究”。

## 必须遵守的框架

- 训练信息受限指训练阶段无法完整调用部分监督、历史样本或跨中心原始数据；“数据不全”仅为背景概括，不是缺失值插补，也不等于物理删除数据。
- 第三章是持续医学图像分割的场景定义、协议、指标与系统实证研究，不是单一算法。
- 第四章是 FedSubMerge：无历史原始样本回放的联邦持续分类；不宣称形式化隐私保证。
- 第五章5.3.1是原始 ZScribbleSeg 静态弱监督分割；5.3.2 ScribbleCL 是未完成的 `TODO-EXPERIMENT` 扩展；5.4是有限回放的 SAMCL 持续配准。
- 第六章为总结与展望。

## 已批准边界与后续准备

1. 第一章及其术语边界已批准，不再列为待复审或待修改任务。
2. 2.1.1 为已验证正文；2.1.2 仍为 `queued`，不得直接写作。
3. 准备 2.1.2 上下文前，必须先核查 `sources/samcl` 和外部原始文献；不得在未核查时生成其完整上下文、引用或结论。
4. ScribbleCL 仍为 `TODO-EXPERIMENT` / `blocked_by_experiments`，不得生成实验内容或性能表述。

## 已验证工程状态

- `main.tex` 只包含六个连续章节；XeLaTeX 全量构建成功。
- `chapters/ch02_foundations.tex` 的2.1.1为已验证内容，未自由重写；2.1.2保持 `queued`。
- 活动引用、标签和 BibTeX 检查通过；参考论文长文本重合审查无命中。
- 完整记录见 `handoff/LATEST_CODEX_REPORT.md`；不得将本上下文包直接复制为论文正文，也不得使用 `sources/reference_thesis/` 正文。

## 允许输出

本上下文包仅记录已批准框架和后续取证前提；在 Codex 完成来源核查并形成新的 2.1.2 证据包前，不输出 2.1.2 学术正文。
