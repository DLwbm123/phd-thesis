# 最新交接：作者 ZIP 集成与发布（2026-08-31）

作者要求把最新 ZIP 更新到 GitHub 和 Overleaf。输入为 `PhD_Thesis_predefense_revised_source.zip`，SHA-256 `94f3c5226dc7d05f379f2ca8de0f770ba2ad74f513d33aa53e1b523f71838659`；原包及解压副本均保留。

## 集成范围

- 原样导入 54 个包内文件，按作者新稿将 FedSubMerge 放在第四章、ScribbleCL/SAMCL 放在第五章；移除 11 个被新路径替代的旧正文/图件路径，保留三个原字体文件。
- 没有润色正文、修改数据或重画图。FedSubMerge 框架 PDF 保持原稿，三张 EPS 按作者文件使用。
- ZIP 文件中仅 `SYNC_STATUS.md` 更新为本地同步说明、`MANIFEST.sha256` 重建为本次文件清单；其余 52 个文件保持字节一致。ZIP 内原始状态文件仍保留在外部解压副本。
- 仓库既有契约、作者协议、来源映射、章节索引和证据定位随当前章序更新；仅调整现有保护检查中的章节文件名。原实验代码和只读论文源材料不在修改范围。

## 核验

完整编译生成 165 页。最终 XeLaTeX 日志无错误、未定义引用、重复标签、缺失字符或 Overfull；保留模板既有的 xdvipdfmx 浮动体对象重复声明提示，输出 PDF 可正常渲染。16 幅编号图、27 张表和 127 条已引用文献均生成。

83 个公式、103 行结果表数字、687 条实验记录保持不变。全部页面缩略图完成版面巡检，关键新稿页面单独查看；第四章长标题自然分为两行，未擅自修改。源材料与七个未提交实验文件在发布前后核对 SHA-256。

本地缺少 Ghostscript 曾导致 EPS 嵌入失败；已安装该编译依赖并从头重编译成功。没有将这一环境问题转化为论文内容改动。风格命中只登记，参考文本重合仅为既有 MICCAI 会议名称。

## 发布与边界

沿用既有两个论文分支与 Overleaf 项目，按本地编译、GitHub、Overleaf、远端回读的顺序非强制发布。`main` 不合并；Overleaf 只同步受管编译输入，研究账本和交接材料保留在 GitHub。具体提交、校验和与本地保护核验见 `latest_zip_sync_20260831/release_receipt.json`。

作者提供的是本次最新版本；工程核验不替代学术终审。包内章节卡及历史说明不是新增工作指令。后续仅响应作者新的修改意见，不能用旧任务或旧章节卡恢复上一版正文。尚未完成的 ScribbleCL Class-CL/Organ-CL、新系统及真实部署实验仍未执行。
