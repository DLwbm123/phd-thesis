Read `AGENTS.md`, `WORKFLOW_MODES.md`, `STATE.md`, the relevant chapter card, and this task. In the first task of a session, or if governance files changed, also read `THESIS_CONTRACT.md`, `AUTHORSHIP_PROTOCOL.md`, `AUTHOR_VOICE.md`, and `PROJECT_CONFIG.md`.

Goal: integrate the approved GPT Pro draft below into the exact thesis subsection.

SECTION ID: {{SECTION_ID}}
TARGET TITLE: {{TITLE}}
TARGET FILE: {{FILE}}
EVIDENCE IDS: {{EVIDENCE_IDS}}

APPROVED LATEX DRAFT
---BEGIN DRAFT---
{{FULL_DRAFT}}
---END DRAFT---

EVIDENCE UPDATES
---BEGIN EVIDENCE---
{{EVIDENCE_ROWS}}
---END EVIDENCE---

Required actions:

1. Inspect the target chapter and replace only the placeholder/content for the specified subsection.
2. Preserve the approved academic wording. Adapt only LaTeX syntax, labels, citation commands, verified terminology, and notation needed for compilation.
3. Verify every citation against the original BibTeX source, DOI/publisher metadata, or primary paper. Add only verified missing entries and deduplicate by DOI/title/key.
4. If this is the first verified citation in the thesis, set `\thesisbibliographytrue` in `config/build_flags.tex`; otherwise preserve the current setting.
5. Append evidence rows without duplicating IDs.
6. Register new terms and symbols.
7. Use the fast-section workflow in `WORKFLOW_MODES.md`: run `bash scripts/verify_fast_section.sh --quiet {{FILE}}`, which performs one complete build plus changed-text style/overlap and metadata checks.
8. Do not mechanically rewrite flagged text; report it for GPT Pro unless the issue is a deterministic typo. Run full-repository style and overlap audits only at a chapter milestone, on explicit request, or if the changed-text audit finds a concern.
9. Audit undefined citations/references, duplicate labels, missing files, TODO markers, and numerical consistency for this change.
10. Update `STATE.md`, `qa/chapter_status.csv`, and a concise `handoff/LATEST_CODEX_REPORT.md` in the content commit. Rebuild `handoff/CONTEXT_PACKET_FOR_GPT.md` only at a chapter boundary or when GPT Pro explicitly requests it. For an Overleaf input change, push once, run the fingerprint-verified `--skip-local-build` synchronization, report the remote SHA in the final response, and do not create a second report-only commit.

Do not edit `sources/`. Do not import wording from the reference thesis. Do not invent facts, citations, numbers, or author metadata. Do not rewrite text merely to influence an AI detector.
