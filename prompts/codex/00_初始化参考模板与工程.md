Read `AGENTS.md`, `THESIS_CONTRACT.md`, `AUTHORSHIP_PROTOCOL.md`, `AUTHOR_VOICE.md`, `REFERENCE_TEMPLATE_POLICY.md`, `PROJECT_CONFIG.md`, `PLANS.md`, `STATE.md`, `SOURCE_MAP.md`, and `chapter_cards/ch01.md` before making changes.

Goal: verify the clean fduthesis project, prepare local source material, compile the thesis, and create the context packet for Chapter 1, Section 1.1.1. Do not draft the academic section.

Tasks:

1. Verify that the repository uses the root `main.tex`, `config/thesis_info.tex`, `chapters/`, and `bibliography/references.bib` supplied by this package.
2. Confirm that only template implementation files and logo assets came from the reference thesis. Search the active thesis files for reference-thesis metadata, title, author names, chapter prose, figure names, bibliography entries, and labels. Remove any accidental content import while preserving the template class files.
3. Inspect `sources/`:
   - identify the actual root of each unpacked project;
   - record verified main TeX and BibTeX files in `PROJECT_CONFIG.md` or `SOURCE_MAP.md`;
   - never modify source files;
   - do not use `sources/reference_thesis/chapter*.tex` as a writing source.
4. Fill any still-unverified project configuration values using actual inspection, including Git branch and local build commands.
5. Build the clean thesis with XeLaTeX/BibTeX using the verified full command. Distinguish template/environment errors from content errors.
6. Run:
   - `python3 scripts/style_audit.py --input chapters --patterns qa/style_red_flags.csv --output qa/style_audit_report.md`
   - `python3 scripts/reference_overlap_audit.py --thesis chapters --reference sources/reference_thesis --output qa/reference_overlap_report.md`
7. Create `handoff/CONTEXT_PACKET_FOR_GPT.md` for Section 1.1.1 containing:
   - the exact purpose and boundaries of the subsection;
   - the current preceding/following headings;
   - verified terminology and citation conventions;
   - relevant passages or source locations from the four user works that motivate medical image segmentation, registration, and classification;
   - a list of external facts that still require primary-source research;
   - no prose from the reference thesis.
8. Update `STATE.md` to show that Section 1.1.1 is ready for GPT Pro drafting.
9. Write `handoff/LATEST_CODEX_REPORT.md` with files inspected/changed, build command/result, source roots, template-content isolation result, QA results, unresolved issues, and recommended next prompt.

Constraints:

- Do not write Section 1.1.1.
- Do not copy any prose, bibliography, figures, metadata, or labels from the reference thesis.
- Do not modify `sources/`.
- Do not invent missing author metadata.
- Do not optimize text for AI-detector scores.
- Keep changes minimal and reviewable.
