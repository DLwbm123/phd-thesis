Read `AGENTS.md`, `THESIS_CONTRACT.md`, `AUTHORSHIP_PROTOCOL.md`, `AUTHOR_VOICE.md`, `REFERENCE_TEMPLATE_POLICY.md`, `PROJECT_CONFIG.md`, `PLANS.md`, `STATE.md`, `SOURCE_MAP.md`, and all current `chapter_cards/chXX.md` before making changes.

This prompt assumes the 2026-07-31 supervisor-framework migration has already been completed. If `main.tex` still includes `ch03_zscribble`, `ch04_benchmark`, `ch05_samcl`, `ch06_fedsubmerge`, or `ch07_conclusion`, stop normal initialization and execute the approved framework-migration task first. Do not draft new academic prose during migration.

Goal: verify the clean fduthesis project, the continuous six-chapter architecture, local source material, build toolchain, and the current GPT context packet.

Required architecture:

```text
Chapter 1 Introduction
Chapter 2 Foundations
Chapter 3 Continual segmentation benchmark
Chapter 4 FedSubMerge
Chapter 5 ScribbleCL and SAMCL
Chapter 6 Conclusion and future work
```

Tasks:

1. Inspect `git status`, branch, remotes, uncommitted changes, and divergence before editing. Never discard local work.
2. Verify the root `main.tex`, `config/thesis_info.tex`, `chapters/`, and `bibliography/references.bib`.
3. Confirm that only template implementation files and logo assets came from the reference thesis. Do not use its prose, bibliography, figures, metadata, or labels as a writing source.
4. Verify `sources/` roots and update `PROJECT_CONFIG.md` / `SOURCE_MAP.md` only from actual inspection. Never modify `sources/`.
5. Confirm the source-to-chapter mapping:
   - Benchmark → Chapter 3;
   - FedSubMerge → Chapter 4;
   - cited static weak-supervision mechanism → Section 5.3.1;
   - ScribbleCL protocol and experiments → Section 5.3.2;
   - SAMCL → Section 5.4.
6. Confirm that the cited static weak-supervision paper is not described as a continual-learning algorithm or thesis contribution, and that unfinished ScribbleCL results remain `TODO-EXPERIMENT`.
7. Build the full thesis with the verified XeLaTeX/BibTeX command. Distinguish environment, template, citation, and content errors.
8. Run the project’s style and reference-overlap audits.
9. Check that the table of contents contains six continuous chapters and no empty Chapter 6 or old Chapter 7.
10. Create or refresh `handoff/CONTEXT_PACKET_FOR_GPT.md` for the current approved writing target. Do not assume Section 1.1.1 is still the target; read `STATE.md`.
11. Update `STATE.md`, `qa/chapter_status.csv`, and `handoff/LATEST_CODEX_REPORT.md` with actual results.

Constraints:

- Do not overwrite a locally newer, verified Section 2.1.1.
- Do not invent title finality; the working title remains an author decision until confirmed.
- Do not invent citations, formulas, experiments, or clinical/privacy claims.
- Do not optimize text for AI-detector scores.
- Keep changes minimal, reviewable, and consistent with the current six-chapter contract.
