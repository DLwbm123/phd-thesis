# 本次修改说明：标题层级、公式标点与插图排版

## 一、标题最多保留三级

正文层级统一为“章—节—小节”，编号最多为 `1.1.1`。原有 20 处第四级标题（含不编号标题）和 8 处段落标题已合并到所属三级小节的正常论述中，共处理 28 处。必要的主题信息改写为段首过渡句，不以加粗小标题或隐去编号的方式保留更深层级。

模板的 `secnumdepth` 与 `tocdepth` 均设为 2（book 类中章为第 0 级），并同步调整不编号章节后的编号层级恢复设置。正文、目录及 PDF 书签均最多保留三级。

## 二、结合句子结构校对公式标点

逐条核对正文 95 处独立公式及其前后文。其中 6 处需要修改，其他标点保留。符号解释或后续运算仍属于同一句时使用逗号；公式结束完整句子时使用句号；公式嵌入尚未结束的句子时不机械添加标点。分段表达式的句末标点移至整个 `cases` 环境之后，不再放在最后一个分支的条件中。

具体修改位置与理由见 `修改记录/本次排版校对记录.md`。数学关系、变量、数值、公式标签和公式数量保持不变。

## 三、放大插图并调整密集图的布局

对全文 20 幅编号插图逐一检查。可编辑示意图扩大到版心宽度，并提高图中文字字号；同时调整文本框、行距、节点位置与箭头，避免字号放大后出现遮挡。外部 PDF 图裁去外围空白，保留原有矢量内容与医学影像，不通过重新绘制或生成影像替换实验结果。

宽幅对比图、矩阵图、框架图及标注序列图采用横向整页排版。图 4.1、图 5.1、图 6.2 和图 6.3 按内容拆分为同一图号下的续图；图 6.2 分别展示两类配准任务，并将方法分成两组放大，每组重复同一固定图像和移动图像作参照。图号及正文交叉引用保留，插图目录不重复列出续图。

正文仍为 A4 纵向，横向页面仅用于宽幅图。放大改善显示和打印尺寸，不代表增加原始医学影像的采样分辨率。

## 四、保留内容与编译

保留上次修改后的章节结构与中英文摘要。三份独立实验结果表、参考文献数据库及公式数学内容均已与本次上传版本对照核验；本次未重跑研究实验，未修改实验数值。

`main.tex` 为编译入口，`main.pdf` 为本次修改后的完整论文。使用包含 XeLaTeX、BibTeX、latexmk 及所需宏包的 TeX 环境，在根目录运行：

```sh
latexmk -xelatex -interaction=nonstopmode -file-line-error main.tex
```

本次使用模板已有的 Fandol 字体回退设置编译，压缩包不附字体文件。使用不同的本地字体重新编译时，换行和页码可能变化。

本次已从无辅助文件的源目录重新编译，并复核修改后图页、公式标点、目录层级与交叉引用。详细校对记录保存在 `修改记录/本次排版校对记录.md`。
