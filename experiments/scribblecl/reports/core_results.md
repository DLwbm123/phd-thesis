# ScribbleCL core run status

This report is generated while core runs are in progress.  It contains no
comparative conclusion until all four methods and all three seeds complete.

Run `python scripts/aggregate_core.py --runs-root "$SCRIBBLECL_OUTPUT_ROOT/core_runs" --results-root results`
to update the lightweight CSV files.  Dense references, independent references
and original ZS static comparisons are intentionally not launched in this run.
