\begingroup
\centering
\begin{tikzpicture}[
  font=\small,
  box/.style={
    rectangle,
    rounded corners=2pt,
    draw=black,
    fill=white,
    text=black,
    line width=0.65pt,
    align=center,
    inner xsep=7pt,
    inner ysep=5pt
  },
  band/.style={
    box,
    fill=black!5,
    text width=13.8cm,
    minimum height=1.05cm
  },
  widebox/.style={
    box,
    text width=13.8cm,
    minimum height=1.08cm
  },
  methodbox/.style={
    box,
    text width=4.15cm,
    minimum height=2.35cm,
    inner ysep=4pt
  },
  arrow/.style={
    -{Latex[length=2.0mm,width=1.3mm]},
    draw=black,
    line width=0.65pt
  },
  relation/.style={
    draw=black,
    line width=0.65pt
  },
  node distance=6.5mm and 6mm
]

\node[band] (constraints) {
  \textbf{现实约束}\\[2pt]
  \footnotesize
  部分监督
  \quad｜\quad
  历史访问受限
  \quad｜\quad
  原始数据不可集中
  \quad｜\quad
  任务与分布持续演化
};

\node[widebox, below=of constraints] (question) {
  \textbf{核心问题}\\[2pt]
  在训练信息受限条件下，如何使医学影像分析模型持续获取新知识并保持既有能力？
};

\node[widebox, below=of question] (benchmark) {
  \textbf{第 3 章：场景与评价基础}\\[2pt]
  持续医学图像分割的场景定义、实验设定与综合评价
};

% 以第 5 章分割节点为中轴放置三个方法框，使整行严格居中。
\node[methodbox, below=9mm of benchmark] (scribble) {
  \textbf{\strut 第 5 章：分割}\\[2pt]
  {\scriptsize\strut 当前标注稀疏}\\[-1pt]
  {\scriptsize\strut 历史样本有限保留}\\[3pt]
  \strut 涂鸦监督持续分割\\[2pt]
  {\footnotesize\strut ScribbleCL}
};

\node[methodbox, left=of scribble] (fed) {
  \textbf{\strut 第 4 章：分类}\\[2pt]
  {\scriptsize\strut 数据不可集中}\\[-1pt]
  {\scriptsize\strut 历史样本不可回放}\\[3pt]
  \strut 联邦持续分类\\[2pt]
  {\footnotesize\strut FedSubMerge}
};

\node[methodbox, right=of scribble] (samcl) {
  \textbf{\strut 第 5 章：配准}\\[2pt]
  {\scriptsize\strut 器官与模态持续变化}\\[-1pt]
  {\scriptsize\strut 历史图像对有限保留}\\[3pt]
  \strut 持续医学图像配准\\[2pt]
  {\footnotesize\strut SAMCL}
};

\coordinate (branch) at ($(benchmark.south)+(0,-4mm)$);

\draw[arrow] (constraints) -- (question);
\draw[arrow] (question) -- (benchmark);
\draw[relation] (benchmark.south) -- (branch);

% 上方分支：横线距 benchmark 4mm，箭头长度为 5mm。
\draw[relation]
  (fed.north |- branch) --
  (samcl.north |- branch);

\draw[arrow]
  (fed.north |- branch) --
  (fed.north);

\draw[arrow]
  (scribble.north |- branch) --
  (scribble.north);

\draw[arrow]
  (samcl.north |- branch) --
  (samcl.north);

% 下方汇合与上方分支保持对称。
\coordinate (merge) at ($(scribble.south)+(0,-4mm)$);

\draw[relation]
  (fed.south) --
  (fed.south |- merge);

\draw[relation]
  (scribble.south) --
  (scribble.south |- merge);

\draw[relation]
  (samcl.south) --
  (samcl.south |- merge);

\draw[relation]
  (fed.south |- merge) --
  (samcl.south |- merge);

\node[band, below=5mm of merge] (goal) {
  \textbf{共同分析主线}\\[2pt]
  \footnotesize
  在不同任务与信息访问条件下，平衡持续学习的稳定性、可塑性与泛化能力
};

\draw[arrow] (merge) -- (goal.north);

\end{tikzpicture}
\par
\endgroup
